# Build artifacts + deps geradas localmente (vão ser geradas dentro do container)
node_modules
.next
out
build
dist

# Versionamento + hooks
.git
.gitignore
.gitattributes
.github

# Env files (NUNCA copiar pra imagem — Easypanel injeta via UI)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editores / OS
.vscode
.idea
.DS_Store
Thumbs.db
*.swp

# Docs grandes (mantém o SETUP_COMPLETO.sql que é referência do build script)
*.md
scratch
.openclaude
.claude

# Atalhos Windows
*.bat
